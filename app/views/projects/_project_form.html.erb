<%= form_for project, remote: true, authenticity_token: true do |f| %>
  <%= f.label :project_name %>
  <%= f.text_field :name, class: "form-control", data: {textarea: "name"} %>

  <%= f.submit "send", data: {send: "name"} %>
<% end %>
