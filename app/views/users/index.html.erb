<ul>
<% @users.each do |u| %>
	<li>
		<%= "#{u.name} #{u.email}"%> | 
		<% if current_user.admin? && !current_user?(user) %>
			<%= link_to "Delete", u, method: :delete, data: {confirm: "You sure?"} %>
		<% end %>
	</li>
<% end %>
</ul>